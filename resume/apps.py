"""App configuration for the resume app."""

from django.apps import AppConfig


class ResumeConfig(AppConfig):
    """Configuration for the resume app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "resume"
